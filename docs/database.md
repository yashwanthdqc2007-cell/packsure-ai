# PackSure AI — Database Design

This document describes the database schema and data model.

See `database/schema.sql` for the actual schema.
See `database/README.md` for setup instructions.

## TODO

- Document table relationships with ER diagram
- Document indexing strategy
- Document data retention policy
- Document RLS (Row Level Security) policy for Supabase
