"""
Rules routes — GET /api/v1/rules

TODO (Backend Dev):
- GET /api/v1/rules — Return applicable rules for a given product category
"""

from fastapi import APIRouter

router = APIRouter(prefix="/rules", tags=["rules"])


# TODO: Implement rules endpoint
