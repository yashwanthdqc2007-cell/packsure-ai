"""
History routes — GET /api/v1/history

TODO (Backend Dev):
- GET /api/v1/history — Return paginated scan history with filters
"""

from fastapi import APIRouter

router = APIRouter(prefix="/history", tags=["history"])


# TODO: Implement history endpoint
